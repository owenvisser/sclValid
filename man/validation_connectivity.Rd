% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_connectivity.R
\name{validation_connectivity}
\alias{validation_connectivity}
\title{Calculate Connectivity}
\usage{
validation_connectivity(clusters, nn, h = 5)
}
\arguments{
\item{clusters}{A vector containing one cluster assignment per observation.}

\item{nn}{A list containing nearest-neighbor indices for each observation.}

\item{h}{Number of nearest neighbors to consider. Defaults to \code{5}.}
}
\value{
A numeric connectivity score. Lower values indicate better
agreement between cluster assignments and local neighborhood structure.
}
\description{
Calculates the Connectivity (CN) measure for a clustering solution using a
nearest-neighbor representation of the data. The measure penalizes nearby
observations that are assigned to different clusters.
}
\examples{
clusters <- c(1, 1, 2, 2)
x <- matrix(
  c(
    0, 0, 3, 3,
    0, 1, 3, 4
  ),
  nrow = 2,
  byrow = TRUE
)
nn <- list(c(2, 3),  c(1, 3),  c(4, 2),  c(3, 2))
validation_connectivity(clusters, nn, h = 2)

}
\references{
Pihur V, Brock GN, Datta S.
Cluster validation for microarray data: an appraisal.
In: Advances in Multivariate Statistical Methods.
World Scientific; 2009:79-94.
}
