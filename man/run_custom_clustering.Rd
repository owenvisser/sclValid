% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering_custom.R
\name{run_custom_clustering}
\alias{run_custom_clustering}
\title{Run a User-Defined Clustering Method}
\usage{
run_custom_clustering(sce, fun, name, ...)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} object containing a \code{"data"} assay.}

\item{fun}{A clustering function. The first argument supplied to \code{fun} is
the numeric data matrix with features in rows and observations in columns.
The function must return one cluster assignment per observation.}

\item{name}{Character string specifying the name of the clustering solution.}

\item{...}{Additional arguments passed to \code{fun}.}
}
\value{
The input \code{SingleCellExperiment} object with the custom clustering
assignments added to \code{colData}.
}
\description{
Applies a user-supplied clustering function to the \code{"data"} assay of a
\code{SingleCellExperiment} object. The clustering function must return one
cluster assignment for each observation in the dataset.
}
\details{
The clustering assignments are stored in \code{colData} using the name supplied
by \code{name}. Information required to rerun the clustering method is retained
internally so that stability-based validation measures can reproduce the
clustering on perturbed datasets.
}
\examples{
sce <- make_sce(example_data, example_labels)

my_kmeans <- function(data, centers) {
  stats::kmeans(t(data), centers = centers)$cluster
}

sce <- run_custom_clustering(
  sce,
  fun = my_kmeans,
  name = "MyKmeans_cs3",
  centers = 3
)

SummarizedExperiment::colData(sce)

}
