% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_average_distance.R
\name{validation_average_distance}
\alias{validation_average_distance}
\title{Calculate Average Distance}
\usage{
validation_average_distance(clusters, reduced_clusters, distmat, index)
}
\arguments{
\item{clusters}{A vector containing cluster assignments obtained from the
full dataset.}

\item{reduced_clusters}{A list of clustering-assignment vectors obtained
from the reduced datasets.}

\item{distmat}{A numeric matrix containing pairwise distances between
observations in the full dataset.}

\item{index}{A list specifying the observations removed for each reduced
clustering solution.}
}
\value{
A numeric average-distance stability score. Lower values indicate
greater agreement between full and perturbed clustering solutions.
}
\description{
Calculates an adapted Average Distance (AD) stability measure by comparing
clustering solutions obtained from the full dataset with corresponding
solutions obtained after perturbing the dataset.
}
\details{
The implementation is adapted from the clustering stability measures
described by Datta and Datta.
}
\examples{
clusters <- c(1, 1, 2, 2)
x <- matrix(
  c(
    0, 0, 3, 3,
    0, 1, 3, 4
  ),
  nrow = 2,
  byrow = TRUE
)

reduced_clusters <- list(
  c(1, 2, 2),
  c(1, 2, 2),
  c(1, 1, 2),
  c(1, 1, 2)
)

index <- as.list(1:4)
distmat <- as.matrix(stats::dist(t(x)))

validation_average_distance(
  clusters,
  reduced_clusters,
  distmat,
  index
)

}
\references{
Adapted from:
Datta S, Datta S. Comparisons and validation of statistical clustering
techniques for microarray gene expression data.
Bioinformatics. 2003;19(4):459-466.
}
